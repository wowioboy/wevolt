
			function onFlashCommand( cmd, param )
			{
				//alert( cmd + " : " + param );
				switch( cmd )
				{
					case "pg_change":	
						alert( "Page changed: " + pararm );
					break;
					
					case "share":
						alert( "Share: " + param );
					break;
					
					case "help":
						alert( "Help" );
					break;
					
					case "buy_now":
						alert( "Buy now: " + param );
					break;
					
				}
			}
			
